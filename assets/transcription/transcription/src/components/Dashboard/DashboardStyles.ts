import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  gridListItem: {
    paddingLeft: "15px",
    paddingRight: "15px",
  },
  introContainer: {
    textAlign: "center",
  },
  notMeLink: {
    color: "#d9534f",
    cursor: "pointer",
  },
});

export default useStyles;

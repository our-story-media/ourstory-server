import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  introContainer: {
    textAlign: "center",
  },
  notMeLink: {
    color: "#d9534f",
    cursor: "pointer",
  },
});

export default useStyles;

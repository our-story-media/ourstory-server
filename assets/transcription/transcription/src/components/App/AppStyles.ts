import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    gridListItem: {
        paddingLeft: '15px',
        paddingRight: '15px',
    }
});

export default useStyles;